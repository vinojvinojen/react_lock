// // Downloads.js
// import React from 'react';
// import Fieldset from './Fieldset';

// export default function Downloads({ macAddress, ipAddress }) {
//   const handleFieldsetSubmit = (legend) => {
//     // console.log(`Submit button clicked (${legend})`);
//     // You can add additional logic here for handling the button click, if needed.
//     if(legend==="Downloads Format"){
//       console.log("hi");
//     }
//   }

//   return (
//     <div>
//       {/* <Fieldset legend="Downloads Format" macAddress={macAddress} ipAddress={ipAddress} onSubmitButtonClick={handleFieldsetSubmit} /> */}
//       <Fieldset legend="Downloads Facility Codes" macAddress={macAddress} ipAddress={ipAddress} onSubmitButtonClick={handleFieldsetSubmit} />
//       <Fieldset legend="Downloads Holiday Data" macAddress={macAddress} ipAddress={ipAddress} onSubmitButtonClick={handleFieldsetSubmit} />
//       <Fieldset legend="Downloads Timezone Data" macAddress={macAddress} ipAddress={ipAddress} onSubmitButtonClick={handleFieldsetSubmit} />
//       <Fieldset legend="Downloads Categories" macAddress={macAddress} ipAddress={ipAddress} onSubmitButtonClick={handleFieldsetSubmit} />
//     </div>
//   );
// }
